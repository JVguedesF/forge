enum WorkoutType { musculacao, corrida, drills, bola, mobilidade, custom }

enum ExerciseType { weightReps, timed, repsOnly, distancePace }

enum WeightUnit { kg, lb }

enum HeightUnit { cm, ft }

enum GoalType { sessions, distance, weight, reps, duration }

enum MacroPhase { base, strength, intensification, deload }
