import 'package:flutter/material.dart';
class MacroScreen extends StatelessWidget {
  const MacroScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    body: Center(child: Text('macro')));
}
